from abc import ABC


class Possession(ABC):
    #Needs to contain/do:
    #Has a Type (→Comparable!)
    #Has a Name
    #Can be owned
    #Can be produced / OR is randomly given
    #Can be consumed / break (True / False + When?)
    #Quality / Productivity / Efficiency
    #Can have fixed / variable coosts
    pass
